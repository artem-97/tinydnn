#include <gtest/gtest.h>

#include <iostream>

#include "../loss.h"

class LossTest : public ::testing::Test {};

TEST_F(LossTest, Ctr) {
  // Linalg::Vector x(3);
  // x[0] = 1.;
  // x[1] = 2.;
  // x[2] = 3.;

  // std::cout << x << std::endl;
  // std::cout << loss::L2::forward(x, x) << std::endl;
  // std::cout << loss::L2::backward(x, x) << std::endl;
}
